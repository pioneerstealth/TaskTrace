document.addEventListener('DOMContentLoaded', () => {
    const createTaskButton = document.querySelector('.create-task-button');
    createTaskButton.addEventListener('click', () => {
        alert('Task creation logic goes here.');
    });
});
